hazelcast:
  cluster-name: ${hazelcast.cluster.name}
  network:
    interfaces:
      enabled: true
      interfaces:
        - 127.0.0.1
    join:
      multicast:
        enabled: true
#      kubernetes:
#        enabled: true



